% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analyses_visualizations.R
\name{mblm_get_coefficients_by_group}
\alias{mblm_get_coefficients_by_group}
\title{Get MBLM Coefficients by Group}
\usage{
mblm_get_coefficients_by_group(dats, x, y, group, repeated = FALSE)
}
\arguments{
\item{dats}{A data frame containing the variables.}

\item{x}{The independent variable.}

\item{y}{The dependent variable.}

\item{group}{The grouping variable. The function will estimate coefficients for each unique value of this group.}

\item{repeated}{Logical, if `TRUE`, the Siegel estimator is used, otherwise the Theil-Sen estimator is applied. Defaults to `FALSE`.}
}
\value{
A tibble with three columns: `group` (unique group values), `coefficient` (estimated slope coefficients), 
and `estimator` (the name of the estimator used).
}
\description{
This function estimates the slope coefficients of a linear relationship between two variables (`x` and `y`)
for each group in a dataset, using either the Siegel or Theil-Sen estimator (from the `mblm` package).
}
\examples{
# Example usage
df <- data.frame(group = rep(c("A", "B"), each = 10), x = rnorm(20), y = rnorm(20))
mblm_get_coefficients_by_group(df, x, y, group, repeated = FALSE)

}
